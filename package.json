{
  "devDependencies": {
    "@monorepolint/core": "^0.4.0",
    "@monorepolint/utils": "^0.4.0",
    "jest-diff": "^25.5.0",
    "lerna": "^3.20.2",
    "monorepolint": "^0.4.0",
    "prettier": "^2.0.5",
    "runtypes": "^4.2.0",
    "typescript-monorepo-toolkit": "^0.0.7"
  },
  "prettier": "./packages/@configscript/config-prettier",
  "private": true,
  "scripts": {
    "build": "tsc -b build-all-tsconfig.json",
    "clean": "rm -rf ./packages/*/{dist,tsconfig.tsbuildinfo} ./packages/@configscript/*/{dist,tsconfig.tsbuildinfo}",
    "fmt": "monorepolint check --fix && prettier --write './**/*.{js,md,ts}'",
    "fmt:check": "monorepolint check && prettier --check './**/*.{js,md,ts}'",
    "lint": "eslint . --ext .ts",
    "prebuild": "typescript-monorepo-toolkit inject-refs -a .",
    "watch": "yarn build -w"
  },
  "workspaces": [
    "packages/*",
    "packages/@configscript/*"
  ]
}
